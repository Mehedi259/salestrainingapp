name: salestrainingapp
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  device_preview: ^1.3.1
  flutter_screenutil: ^5.9.3
  cached_network_image: ^3.4.1
  internet_connection_checker: ^3.0.1
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner:
  flutter_gen_runner:
  flutter_gen:

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
